{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}

    <div class="container py-5">
        <div class="row">
    {% for partner in partners|slice(0, 6) %}
            <div class="col-2">
                <img src="{{ partner.image }}">
            </div>
    {% endfor %}
        </div>
    </div>




{% endblock %}